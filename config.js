// config.js – The Foundation of Limitless
module.exports = {
    // Command prefix – null = prefixless mode
    prefix: null,

    // Owner number(s) – used for special permissions and welcome DM target
    // Format: full number with country code, e.g., "12345678901"
    owner: [
        "12345678901" // Replace with your actual number
    ],

    // Sudo users – additional trusted users with elevated permissions
    sudo: [
        "98765432109"
    ],

    // Welcome message for new group members (placeholder: @user)
    welcome: 'Welcome @user! You have entered the Soul Society.',

    // Goodbye message
    goodbye: 'Goodbye @user! May your path be clear.'
};